$kind: http-request
description: Возвращает список всех групп материалов
url: "{{baseUrl}}/api/material-groups/"
method: GET
headers:
  Accept: application/json
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/Список групп.resources/examples
order: 3000
