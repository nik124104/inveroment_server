$kind: http-request
description: Возвращает древовидную структуру групп материалов
url: "{{baseUrl}}/api/material-groups/tree"
method: GET
headers:
  Accept: application/json
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/Дерево групп.resources/examples
order: 1000
