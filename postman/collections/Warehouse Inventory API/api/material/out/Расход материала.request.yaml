$kind: http-request
description: Списание материала со склада. Проверяет наличие достаточного остатка.
url: "{{baseUrl}}/api/material/out"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
body:
  type: json
  content: |-
    {
      "material_id": 1245,
      "quantity": 376.787716409822,
      "comment": null
    }
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/Расход материала.resources/examples
order: 1000
