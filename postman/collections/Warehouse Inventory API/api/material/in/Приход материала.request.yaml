$kind: http-request
description: Оприходование материала на склад. Увеличивает остаток.
url: "{{baseUrl}}/api/material/in"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
body:
  type: json
  content: |-
    {
      "material_id": 1245,
      "quantity": 376.787716409822,
      "comment": null
    }
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/Приход материала.resources/examples
order: 1000
