$kind: http-request
description: Возвращает материалы, у которых остаток ниже минимального.
url: "{{baseUrl}}/api/stock/low"
method: GET
headers:
  Accept: application/json
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/Критический остаток.resources/examples
order: 1000
