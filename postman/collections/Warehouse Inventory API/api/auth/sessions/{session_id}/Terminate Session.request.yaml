$kind: http-request
description: Завершить указанную сессию.
url: "{{baseUrl}}/api/auth/sessions/:session_id"
method: DELETE
headers:
  Accept: application/json
pathVariables:
  session_id: "9831"
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/Terminate Session.resources/examples
order: 1000
