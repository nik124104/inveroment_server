$kind: http-request
description: Список активных сессий.
url: "{{baseUrl}}/api/auth/sessions"
method: GET
headers:
  Accept: application/json
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/Get My Sessions.resources/examples
order: 2000
