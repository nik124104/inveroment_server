$kind: http-request
description: Выход на всех устройствах.
url: "{{baseUrl}}/api/auth/logout/all"
method: POST
headers:
  Accept: application/json
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/Logout All Devices.resources/examples
order: 1000
