$kind: http-request
description: Информация о текущем пользователе.
url: "{{baseUrl}}/api/auth/me"
method: GET
headers:
  Accept: application/json
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/Get Current User Info.resources/examples
order: 1000
